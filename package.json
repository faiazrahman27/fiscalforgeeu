{
  "name": "invoice-lantern-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:tax-engine": "npm --prefix packages/tax-engine run build",
    "build:invoice-core": "npm --prefix packages/invoice-core run build",
    "build:ubl": "npm --prefix packages/ubl run build",
    "build:packages": "npm run build:tax-engine && npm run build:invoice-core && npm run build:ubl",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "npm --prefix apps/api run dev",
    "build:web": "npm run build:packages && npm --prefix apps/web run build",
    "build:api": "npm --prefix apps/api run build",
    "build": "npm run build:packages && npm run build:api && npm --prefix apps/web run build",
    "check:tax-engine": "npm --prefix packages/tax-engine run check",
    "check:invoice-core": "npm --prefix packages/invoice-core run check",
    "check:ubl": "npm --prefix packages/ubl run check",
    "check:packages": "npm run check:tax-engine && npm run check:invoice-core && npm run check:ubl",
    "check:api": "npm run build:packages && npm --prefix apps/api run check",
    "check:web": "npm run build:packages && npm --prefix apps/web run build",
    "check": "npm run check:packages && npm run check:api && npm --prefix apps/web run build",
    "test:tax-engine": "npm --prefix packages/tax-engine run test",
    "test:invoice-core": "npm --prefix packages/invoice-core run test",
    "test:ubl": "npm --prefix packages/ubl run test",
    "test:api": "npm run build:packages && npm --prefix apps/api run test",
    "test": "npm run test:tax-engine && npm run test:invoice-core && npm run test:ubl && npm run test:api",
    "start:web": "npm --prefix apps/web run start",
    "start:api": "npm --prefix apps/api run start"
  }
}
